using System;

// Console application that calculates and prints the square root of the number 12345;

class SquareRoot
{
    public static void Main()
    {
        Console.WriteLine(Math.Sqrt(12345));

        // Here's a second way that we can calculate this: 

        // double SquareRootOfNumber = Math.Sqrt(1234);
        // Console.WriteLine(SquareRootOfNumber);
    }
}